{% extends 'base.html.twig' %}

{% block body %}

    {#<div class="navigation text-center">#}
        {#{{ knp_pagination_render(pagination) }}#}
    {#</div>#}

    <table class="table table-bordered">
<thead>
    <tr>
        {#<th>{{ knp_pagination_sortable(pagination, 'Name'|trans, 't.Name') }}</th>#}
        {#<th>{{ knp_pagination_sortable(pagination, 'Life'|trans, 't.Life') }}</th>#}
        {#<th>{{ knp_pagination_sortable(pagination, 'Experience'|trans, 't.Experience') }}</th>#}
        {#<th>{{ 'Actions'|trans }}</th>#}
        <th>{{  'Name'|trans }}</th>
        <th>{{ 'Life'|trans  }}</th>
        <th>{{  'Experience'|trans }}</th>
        <th>{{ 'Actions'|trans }}</th>
    </tr>
</thead>
<tbody>
    <tr>
{% for monster in monsters %}
<tr>
    <td>{{ monster.name }}</td>
    <td>{{ monster.health }}</td>
    <td>{{ monster.experience }}</td>
    <td>
        <a href="/{{ monster.id }}" class="btn btn-dark">{{ 'Show'|trans }}</a>
        <a href="/edit/{{ monster.id }}" class="btn btn-light border border-secondary">{{ 'Edit'|trans }}</a>
        <a href="/delete/{{ monster.id }}" onclick="return confirm('Are you sure you want to delete {{ monster.name }}?')" class="btn btn-danger ml-1 delete-monster" data-id=>
            {{ 'Delete'|trans }}</a>
    </td>

</tr>
</tbody>
{% endfor %}
</tr>
    </table>

    {#<div class="navigation text-center">#}
        {#{{ knp_pagination_render(pagination) }}#}
    {#</div>#}
{% endblock %}