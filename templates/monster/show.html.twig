{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ monster.name }}</h1>
    <h3>Health {{ monster.health }}</h3>
    <h3>Experience {{ monster.experience }}</h3>
    <h3>Autor: {{ monster.author.firstname }}</h3>
    <a href="{{ url('monster_index') }}" class="btn btn-primary" style="margin-bottom: 10px">Powrót</a>

    {#<div>#}
        {#<img alt="" src="{{ asset('uploads/photos/'~ monster.photo.file.filename) }}">#}
    {#</div>#}
    {% if monster.photo is defined and monster.photo|length %}
        <div>
            <img alt="" src="{{ asset('uploads/photos/'~ monster.photo.file.filename) }}">
        </div>
    {% else %}
        {% include 'default/_single_flash_message.html.twig' with {
            type: 'warning',
            message: 'message.item_not_found'|trans
        } %}
    {% endif %}



    {#<a href="{{ url('photo_new', {id: monster.id}) }}">Add photo</a>        // do usuniecia#}

{% endblock %}