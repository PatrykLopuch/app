{% extends 'base.html.twig' %}

{% block title %} Edit User {% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <br>
    <a href="{{ url('user_index') }}" class="btn btn-primary">Powrót</a>
{% endblock %}